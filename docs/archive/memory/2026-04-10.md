# 2026-04-10

- O hub depende de `Hermes` web (`8501`) e API (`8502`) ativos para a validacao full com `DTIC/new-ticket`.
- A validacao Playwright full tambem depende de `SMOKE_USERNAME` e `SMOKE_PASSWORD` presentes na sessao atual; nao gravar isso em arquivo.
- `DTIC/new-ticket` tinha um bug real de rendering: o chat exibia markdown cru do agente (`**#123**`). A normalizacao em `DticAgentChatEntry.tsx` precisa remover esse markdown residual antes de renderizar o texto.
- O submit `SIS` via FormCreator pode falhar com `Server disconnected without sending a response.` no upstream. O retry de `create_item()` em `httpx.TransportError` estabilizou o smoke `hub-sis-followup-attachment-clean`.
- Para `gestao-carregadores-oficial`, a ordem correta e usar um prompt operacional duro primeiro e consolidar a skill frontend compartilhada so depois de provar as regras em uma segunda base real.
- O novo seletor de servicos do `SIS/new-ticket` introduziu uma carga assíncrona mais rica do catalogo. Os smokes precisaram esperar `categories` + `forms` + render de `.service-card` antes de interagir com a etapa 1.
- O login do gateway apresentou flake intermitente de navegação para `/selector` durante a suite full. O helper `loginThroughGateway()` ficou com duas tentativas controladas para nao mascarar regressao persistente, mas absorver a falha transitória.
