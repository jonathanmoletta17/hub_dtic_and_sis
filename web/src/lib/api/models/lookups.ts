export interface LookupOption {
  id: number;
  name: string;
  completename?: string;
  label: string;
}

export interface TechnicianOption {
  id: number;
  name: string;
  login: string;
  label: string;
}

export interface UserOption {
  id: number;
  name: string;
  login: string;
  label: string;
}
